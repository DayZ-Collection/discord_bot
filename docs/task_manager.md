# TaskManager (System Logic & UX)

## Концепция
Интерактивная панель задач, минимизирующая контекстное переключение разработчика. Все операции (создание, апдейт, вложения) происходят внутри Discord.

## Архитектура данных (ITask)
```typescript
interface ITask {
  id: string;             // Формат TZ-XXXX
  title: string;          // Краткое название
  description: string;    // Подробное описание (.md поддерживается)
  priority: 'LOW' | 'MEDIUM' | 'HIGH';
  status: 'TODO' | 'IN_PROGRESS' | 'DONE' | 'ARCHIVED';
  creatorId: string;      // ID создателя
  assigneeId?: string;    // ID исполнителя
  attachments: string[];  // Ссылки на прикрепленные файлы
  createdAt: Date;
  updatedAt: Date;
}
```

## Визуальная Иерархия (UX)
1. **Header**: ID задачи и индикатор приоритета (ANSI).
2. **Body**: Основной текст с использованием сепараторов `»` и `└─`.
3. **Footer**: Таймстемпы и метаданные.
4. **Controls**: 
   - 🟢 `Take Task` (Назначить себя)
   - 🔄 `Update Status` (Циклическое переключение)
   - 📁 `Add Files` (Модальное окно/загрузка)

## Жизненный цикл задачи
1. Создание через `/task create` -> Открытие **Discord Modal** для ввода данных.
2. Обработка данных в `TaskEngine` -> Генерация ID (TZ-XXX) -> Сохранение в `data/tasks.json`.
3. Генерация визуальной карточки через `TaskEmbedBuilder`.
4. Взаимодействие через Buttons -> Обновление `ITask` в БД -> Редактирование оригинального сообщения.

## Хранение данных (Storage)
На текущем этапе используется локальное JSON-хранилище (`data/tasks.json`). Это обеспечивает:
- Высокую скорость чтения/записи.
- Простоту бэкапа вместе с файлами игрового сервера.
- Отсутствие зависимости от внешних БД.
