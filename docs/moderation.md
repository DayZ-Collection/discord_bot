# Модерация (Security & Enforcement)

## Концепция
Система быстрого реагирования на нарушения. Каждое действие должно быть обосновано, залогировано и донесено до нарушителя в эстетичной форме.

## Команды и Логика
| Команда | Действие | Параметры |
| :--- | :--- | :--- |
| `/kick` | Исключение | `@user`, `reason` |
| `/ban` | Перманентный бан | `@user`, `reason`, `delete_messages` |
| `/mute` | Timeout (v14+) | `@user`, `duration` (1m, 1h, 1d), `reason` |

## User Intelligence (Entry Logging)
Каждый новый участник сервера должен быть зафиксирован в канале `LOG_CHANNEL_ID`. 
Лог входа включает:
- **Full Identity:** Username + ID.
- **Visuals:** Аватар пользователя.
- **Account Age:** Дата создания аккаунта (для выявления твинков/ботов).
- **Server Stats:** Общее количество участников после входа.
2. **Исполнение:** Выполнение действия через Discord API или передача команды на сервер DayZ.
3. **Уведомление (DM):** Отправка пользователю карточки с причиной наказания.
4. **Кросс-платформенное Логирование:** Отправка детального отчета в `LOG_CHANNEL_ID`.

## Структура Global Logs (Unified Format)
Логи разделяются по источнику события с помощью префиксов:
- `[ DISCORD ]` — Действия внутри Discord сервера.
- `[ GAME ]` — (Future) Действия внутри игрового сервера DayZ.

Каждое сообщение в логах должно содержать:
- Тип действия (BAN/KICK/MUTE/WARN).
- Субъект и объект (кто и кого).
- Платформу совершения действия.
- Прямую ссылку на контекст (если применимо).

## Визуализация логов (ANSI)
Логи в канале администраторов используют цветовое кодирование:
- `[1;31m[ BAN ][0m` — Красный
- `[1;33m[ KICK ][0m` — Оранжевый
- `[1;34m[ MUTE ][0m` — Синий
